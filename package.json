{
  "name": "ios-defects-app-server",
  "version": "1.0.0",
  "description": "This is the server component for defects app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "z.ren",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^5.13.8"
  }
}
